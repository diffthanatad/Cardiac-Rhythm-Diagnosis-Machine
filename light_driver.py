import os

# access the terminal and run the following command
# first, use the "make" command to create ready-to-use assembly file
os.system("make")

# run the ready-to-use assembly file with the super user level
os.system("sudo ./ass_light")

# terminal command for running the python file through terminal
##python light_driver.py